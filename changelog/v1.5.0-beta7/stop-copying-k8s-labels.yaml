changelog:
- type: FIX
  description: >
    Stop copying Kubernetes labels when creating Upstreams. Treat labels instead
    as an additional type of metadata that can be explicitly used downstream (e.g. by FDS). 
  issueLink: https://github.com/solo-io/gloo/issues/2634
