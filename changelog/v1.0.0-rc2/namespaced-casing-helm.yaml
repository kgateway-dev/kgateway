changelog:
  - type: HELM
    description: >
      Helm value update: `global.glooRbac.Namespaced` becomes `global.glooRbac.namespaced`.
      This is not a breaking change because helm expects lowercased values.
    issueLink: https://github.com/solo-io/gloo/issues/1569
  - type: NON_USER_FACING
    description: >
      "Update helm values docs during docs generation"
