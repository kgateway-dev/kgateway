changelog:
  - type: FIX
    issueLink: https://github.com/solo-io/gloo/issues/3030
    description: Fixes a bug where the help comment in the stats would be picked up as an actual stat, causing the rate limit connected state check to fail. 
