changelog:
- type: FIX
  description: >
    If we are searching for resources (e.g. upstreams) in a nonexistent namespace, 
    we now return a helpful error.
  issueLink: https://github.com/solo-io/gloo/issues/1647


