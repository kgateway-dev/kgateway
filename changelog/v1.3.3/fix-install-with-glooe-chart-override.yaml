changelog:
  - type: FIX
    description: fix `glooctl install ingress/knative` to nest helm overrides under "gloo" when a GlooE chart is passed with `-f` flag
    issueLink: https://github.com/solo-io/gloo/issues/1999
