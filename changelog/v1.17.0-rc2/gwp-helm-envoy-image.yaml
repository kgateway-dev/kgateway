changelog:
  - type: HELM
    description: >-
      Rename the kube gateway envoy container image helm value from `kubeGateway.gatewayParameters.glooGateway.image` to
      `kubeGateway.gatewayParameters.glooGateway.envoyContainer.image`.
