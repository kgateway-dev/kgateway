changelog:
  - type: HELM
    description: >
      Add quote to envoyExtgraArgs in the gatewayProxy deployment. This allows using values that 
      start with %. This is useful for stackdriver log ingestion
