changelog:
- type: DEPENDENCY_BUMP
  dependencyOwner: solo-io
  dependencyRepo: envoy-gloo
  dependencyTag: NOT_YET_KNOWN
  description: >
   Update envoy-gloo to NOT_YET_KNOWN, pulling in changes that add 
   queryStringParameters, multiValueQueryStringParameters, and
   multiValueHeaders fields to headerBodyTransform output when
   `add_request_metadata` is enabled.
  issueLink: https://github.com/solo-io/solo-projects/issues/4571