changelog:
  - type: FIX
    description: >-
      Avoid allowing glooctl to install v2 variants without an explicit call for it.
      This may be reverted later once we have a better understanding of upgrade path.
      
      skipCI-kube-tests:true
      skipCI-docs-build:true