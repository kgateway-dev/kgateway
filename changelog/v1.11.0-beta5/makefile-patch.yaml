changelog:
- type: NON_USER_FACING
  description: added `install-go-tools` to `generated-code`.  This resolves a coulpe of cases where local builds could fail unexpectedly, or have generated code be _moderately_ different than in CI.