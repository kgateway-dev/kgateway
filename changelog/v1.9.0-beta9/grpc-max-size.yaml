changelog:
  - type: FIX
    issueLink: https://github.com/solo-io/gloo/issues/5085
    description: >
      Change `validationServerGrpcMaxSize` from int64 to int32, and add it to
      values.go, values-template.yaml, and use it in our helm template.