kind: Gateway
apiVersion: gateway.networking.k8s.io/v1
metadata:
  name: example-gateway
  namespace: gwtest
spec:
  gatewayClassName: kgateway
  listeners:
  - protocol: HTTP
    port: 8080
    name: http
    allowedRoutes:
      namespaces:
        from: All
---
apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: gwtest
  labels:
    app: backend
    service: backend
spec:
  ports:
    - name: https
      port: 443
      targetPort: 8443
      appProtocol: https
  selector:
    app: backend
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: backend-route
  namespace: gwtest
spec:
  parentRefs:
    - name: example-gateway
  hostnames:
    - "example.com"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: backend-service
          port: 443
---
# Secret whose ca.crt is not a PEM certificate. Envoy would reject a cluster
# using it as the trusted CA, so the policy must report an error instead.
apiVersion: v1
kind: Secret
metadata:
  name: invalid-ca-secret
  namespace: gwtest
type: Opaque
data:
  ca.crt: bm90IGEgUEVNIGNlcnRpZmljYXRl
---
kind: BackendConfigPolicy
apiVersion: gateway.kgateway.dev/v1alpha1
metadata:
  name: invalid-backend-config-policy
  namespace: gwtest
spec:
  targetRefs:
    - name: backend-service
      group: ""
      kind: Service
  tls:
    secretRef:
      name: invalid-ca-secret
    sni: test.example.com
