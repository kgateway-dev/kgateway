package constants

// AllowEmpty is a reusable term to avoid setting defaults.
// This should be used where gloo decides to set common sense defaults but needs a way to specify a empty set.
const AllowEnabled = "allow_empty"
