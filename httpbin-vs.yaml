apiVersion: gateway.solo.io/v1
kind: VirtualService
metadata:
  name: testrunner
  namespace: gloo-system
spec:
  virtualHost:
    domains:
    - '*'
    routes:
    - matchers:
      - prefix: /
      routeAction:
        single:
          upstream:
            name: gloo-system-httpbin-8000
            namespace: gloo-system
