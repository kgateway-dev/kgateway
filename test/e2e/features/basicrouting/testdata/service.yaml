# example-svc (ns nginx) selects the shared nginx pod; default-ns routes reach it cross-namespace.
apiVersion: v1
kind: Service
metadata:
  name: example-svc
  namespace: nginx
spec:
  selector:
    app.kubernetes.io/name: nginx
  ports:
    - protocol: TCP
      port: 8080
      targetPort: http-web-svc
