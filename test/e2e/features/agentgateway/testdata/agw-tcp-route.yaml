apiVersion: gateway.networking.k8s.io/v1alpha2
kind: TCPRoute
metadata:
  name: tcp-app-1
  namespace: agentgateway-base
spec:
  parentRefs:
    - name: gateway
  rules:
    - name: test
      backendRefs:
        - name: backend
        # Using port 80,since the test expects status code 200
          port: 80