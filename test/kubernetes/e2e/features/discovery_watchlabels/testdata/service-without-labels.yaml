apiVersion: v1
kind: Service
metadata:
  labels:
    bonusKey: bonusValue
  name: example-svc-no-labels
spec:
  ports:
    - name: http
      port: 8000
      targetPort: 8080
  selector:
    selectorKey: selectorValue
